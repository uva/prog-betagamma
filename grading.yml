grades:
    module1:
        type: float
        subgrades:
            scope: integer
            correctness: integer
        calculation: (scope * correctness / 25.0 * 9 + 1).round(1)
        hide_calculated: true
    module2:
        type: float
        subgrades:
            scope: integer
            correctness: integer
        calculation: (scope * correctness / 25.0 * 9 + 1).round(1)
        hide_calculated: true
    module3:
        type: float
        subgrades:
            scope: integer
            correctness: integer
        calculation: (scope * correctness / 25.0 * 9 + 1).round(1)
        hide_calculated: true
    module4:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * correctness / 25.0 * 9 + 1).round(1)
        hide_calculated: true
    style:
        type: float
        subgrades:
            design: integer
            style: integer
        calculation: (design * style / 25.0 * 9 + 1).round(1)
        hide_calculated: true
    tentamen:
        type: float
        subgrades:
            points: integer
        calculation: ((points-13.0)*(4/6)+6).round(1)
    hertentamen:
        type: float
        subgrades:
            points: integer
        calculation: points/100.0

calculation:
    resit:
        hertentamen: 40
        modules: 40
        style: 20
    normal:
        tentamen: 40
        modules: 40
        style: 20

modules:
    submits:
        module1: 1
        module2: 1
        module3: 1
        module4: 1

style:
    submits:
        style: 1

tentamen:
    required: true
    submits:
        tentamen: 1

hertentamen:
    required: true
    submits:
        hertentamen: 1
